@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
@*<--> Model </-->*@
<div class="modal fade" id="fmModal_AssignPermission" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="" role="main">
                    <div class="">
                        <div class="row">
                            <div class="col-md-12 col-sm-12 col-xs-12">
                                <div class="">
                                    <input type="hidden" id="btn_UserId" />
                                    <table class="table table-bordered tree" id="tblFunction">
                                        <thead>
                                            <tr>
                                                <th>Function</th>
                                                <th>Create</th>
                                                <th>Update</th>
                                                <th>View</th>
                                                <th>Delete</th>
                                            </tr>
                                        </thead>
                                        <tbody id="tbl_Content_AssignPermission">
                                            
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="button" id="btn_SavePermission" class="btn btn-primary">Save changes</button>
            </div>
        </div>
    </div>
</div>
<script id="table_template_AssignPermission" type="x-tmpl-mustache">
    <tr class="treegrid-{{Id}}">
        <td>{{Name}}</td>
        <td>
            <input type="checkbox" class="ckAdd" value="{{ParentId}}" id="{{FnId}}"/>
            <label>Allow</label>
        </td>
        <td>
            <input type="checkbox" class="ckUpdate" value="{{ParentId}}" id="{{FnId}}" />
            <label>Allow</label>
        </td>
        <td>
            <input type="checkbox" class="ckView" value="{{ParentId}}" id="{{FnId}}" />
            <label>Allow</label>
        </td>
        <td>
            <input type="checkbox" class="ckDelete" value="{{ParentId}}" id="{{FnId}}" />
            <label>Allow</label>
        </td>
    </tr>
</script>